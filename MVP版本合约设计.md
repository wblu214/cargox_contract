在 MVP 版本中，重点是确保 PicWe 商品信用网络（CCN）能够实现其核心功能的基本流程，并且能够展示完整的支付与融资闭环。为了确保快速迭代和及时交付，合约层的开发可以从以下几个关键功能入手：

### MVP 版本合约设计

#### 1. **CommodityAssetRegistry 合约**

**功能：**

- **资产登记**：允许商户或供应商在区块链上注册商品资产，创建与商品相关的唯一 ID。
- **数据记录**：将商品的基本信息（例如商品类型、数量、价值、运输状态等）记录到合约中。
- **状态管理**：跟踪商品的状态，例如“在途”（In-Transit）或“已交付”（Delivered）。

**MVP 目标：**

- 实现商品的唯一标识符和基本属性的记录。
- 支持资产状态的管理，并允许用户（商户、供应商）查询商品的状态。

**示例功能**：

- 商户上传商品信息（如铜、1000 吨、价值 1000 万美金），生成`Asset ID`，用于后续融资和支付流程。

---

#### 2. **ReceivablePool 合约**

**功能：**

- **应收账款的注册**：允许商户创建基于已注册商品的应收账款代币（例如发票），该发票将与 CommodityAssetRegistry 中的资产 ID 绑定。
- **融资申请**：商户可以通过抵押应收账款（发票）来申请链上融资。
- **LP 参与**：允许链上用户（Liquidity Providers，LP）向应收账款融资池存入 USDT，参与融资。

**MVP 目标：**

- 实现应收账款的代币化和融资池的创建。
- 支持商户通过已注册的商品和应收账款向 LP 发起融资请求。

**示例功能**：

- 商户为 1000 吨铜的交易生成应收账款，绑定发票并申请融资，LP 存入 USDT 进行融资。

---

#### 3. **ClearanceMock 合约**

**功能：**

- **模拟支付清算**：在商户与买家完成交易后，触发清算操作，将 LP 的资金回款，并生成对应的资产返还。
- **支付确认**：记录买家支付状态并更新融资池中的资产状态。
- **自动结算**：完成支付后，LP 获得收益，并自动清算贷款。

**MVP 目标：**

- 实现买家支付确认的触发与融资池的清算功能，确保资金流动和结算的自动化。

**示例功能**：

- 买家完成支付后，触发“买家付款”按钮，ClearanceMock 合约自动结算并将 LP 的本金与收益支付给参与者。

---

### 合约的最小功能集

1. **CommodityAssetRegistry**
   - 注册商品信息
   - 跟踪商品状态
   - 生成唯一资产 ID
2. **ReceivablePool**
   - 创建应收账款代币（与商品 ID 绑定）
   - 允许商户抵押应收账款进行融资
   - LP 参与融资池并存入资金
3. **ClearanceMock**
   - 模拟买家付款
   - 自动清算 LP 资金并生成收益
   - 更新资产状态为“已清算”

### 合约设计中的重点：

- **简化流程**：MVP 版本中的合约主要集中在核心的支付和融资流程上，避免过度复杂的功能，确保可以快速进行演示和验证。

### 重点考虑的安全性和合规性：

- **权限控制**：合约应确保只有授权的商户和用户可以执行相应操作，如资产注册、融资申请等。
- **清算过程的透明性**：确保所有的清算过程和支付分润都能在链上公开，便于审计。

### MVP 合约开发步骤：

1. **开发 CommodityAssetRegistry 合约**，实现商品注册与状态管理。
2. **开发 ReceivablePool 合约**，允许商户抵押应收账款进行融资。
3. **开发 ClearanceMock 合约**，模拟支付清算和自动结算功能。
4. 进行单元测试，确保合约功能正常运行并与前端系统无缝对接。

这样设计的 MVP 合约模块可以确保 PicWe 商品信用网络在黑客松期间能够展示核心功能，包括商品注册、融资池管理和支付清算过程，并为后续版本扩展多链支持和 AI 风控系统做好基础准备。
